footer.container
  .row
    .col-lg-12
      p.text-center
        | Copyright © Cucumber Ltd 2014 -
        a href="/press.html"  Press
      p.text-center
        a href="tel:+442087206842" +44 20 8720 6842 (Europe)
      p.text-center
        a href="tel:+18444144961" 1-844-414-4961 (USA)
      p.text-center#js-edit style="display:none"
        a href=(edit_url(locals['template_path'])) Edit this page
        javascript:
          document.onkeydown = function(evt) {
            evt = evt || window.event;
            if (evt.ctrlKey && evt.keyCode == 69 /* e */) {
              document.getElementById('js-edit').style.display = 'block';
            }
          };

script src="/js/config.js"
script src="/js/main.js"
script src="//cdn.jsdelivr.net/jquery.slick/1.4.1/slick.min.js"
javascript:
  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
  (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
  m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
  })(window,document,'script','//www.google-analytics.com/analytics.js','ga');
  ga('create', 'UA-43811306-1', 'cukes.info');
  ga('send', 'pageview');

  $(document).ready(function () {
    $('body').scrollspy({ target: '#side-nav' });

    // Code carousels
    var ignoreSlickEvents = false;
    $('.carousel').slick({
      autoplay: false,
      adaptiveHeight: true,
      draggable: false,
      swipe: true
    }).on('beforeChange', function (event, slick, currentSlide, nextSlide) {
      if(nextSlide == currentSlide) return;
      // TODO: Be smarter about transitioning when user clicks the control.
      var method;
      if (nextSlide == 0) {
        method = currentSlide == 1 ? 'slickPrev' : 'slickNext';
      } else {
        method = (nextSlide - currentSlide == 1) ? 'slickNext' : 'slickPrev';
      }
      if(ignoreSlickEvents) return;
      ignoreSlickEvents = true;
      $('.carousel').slick(method);
      ignoreSlickEvents = false;
    });

    $('.carousel-control').append('<span data-slide="0">Ruby</span>');
    $('.carousel-control').append('<span data-slide="1">Java</span>');
    $('.carousel-control').append('<span data-slide="2">JavaScript</span>');
    $('.carousel-control').append('<span data-slide="3">Python</span>');
    // Carousel control
    $('.carousel-control span').click(function (e) {
      var slide = parseInt(e.target.dataset.slide);
      $('.carousel').slick('slickGoTo', slide);
    });
  });
